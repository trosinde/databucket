[project]
name = "databucket-mcp-server"
version = "0.1.0"
description = "MCP server for databucket S3 operations"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "boto3>=1.35.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
